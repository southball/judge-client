import * as React from 'react';

const NowLoading = () => (
    <div>
        <span className="loader" style={{display: "inline-block"}} />&nbsp;
        Loading...
    </div>
);

export default NowLoading;
